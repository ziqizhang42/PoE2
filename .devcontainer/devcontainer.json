{
  "$schema": "https://raw.githubusercontent.com/devcontainers/spec/main/schemas/devContainer.schema.json",
  "name": "PoE2",

  "dockerComposeFile": ["../compose.yaml"],
  "service": "tooling",
  "workspaceFolder": "/app",

  // `tooling` sits behind the `tools` profile; naming it here guarantees
  // Compose starts it rather than filtering it out.
  "runServices": ["db", "tooling"],

  // /app/node_modules is a named volume, so it is empty on a fresh clone.
  "postCreateCommand": "pnpm install --frozen-lockfile",

  "customizations": {
    "vscode": {
      "extensions": ["TypeScriptTeam.native-preview", "oxc.oxc-vscode", "vitest.explorer"],
      "settings": {
        // TypeScript 7 uses native-preview with the workspace compiler; its
        // platform package is hoisted by pnpm-workspace.yaml.
        "js/ts.experimental.useTsgo": true,
        "js/ts.tsdk.path": "./node_modules/typescript",

        // Keep NodeNext-generated relative imports on their required .js suffix.
        "js/ts.preferences.importModuleSpecifierEnding": "js",

        "editor.formatOnSave": true,
        "[typescript]": { "editor.defaultFormatter": "oxc.oxc-vscode" },
        "[javascript]": { "editor.defaultFormatter": "oxc.oxc-vscode" },
        "[json]": { "editor.defaultFormatter": "oxc.oxc-vscode" },
        "[jsonc]": { "editor.defaultFormatter": "oxc.oxc-vscode" },

        // Avoid watching generated and dependency trees over the bind mount.
        "files.watcherExclude": {
          "**/node_modules/**": true,
          "**/dist/**": true,
          "**/.vite/**": true
        },
        "search.exclude": {
          "**/node_modules": true,
          "**/dist": true,
          "**/pnpm-lock.yaml": true
        }
      }
    }
  }
}
